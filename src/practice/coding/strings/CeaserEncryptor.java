package practice.coding.strings;

public class CeaserEncryptor {

    public static String caesarCypherEncryptor(String str, int key) {
        int len = str.length();
        for(int i = 0; i < len; i++) {
            //str.charAt(i) += key;
        }
        return str;
    }
}
